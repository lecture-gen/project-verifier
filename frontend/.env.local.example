# 브라우저에서 호출 가능한 백엔드 base URL. 로컬은 http://localhost:8000, 운영은 https://<도메인>.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Next.js 서버 컴포넌트 / Route Handler에서 백엔드를 직접 호출할 때 사용. docker network 안에서는 http://api:8000.
INTERNAL_API_BASE_URL=http://localhost:8000
